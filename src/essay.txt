Understanding what props and state are and the differences between them is a big step towards learning React. State is an updatable structure which is used for containing data or information about the component. The state in a component can be changed over time. While Props are the read-only components. It is an object which stores the attribute value of a tag, and its work is similar to the HTML elements. It helps to pass the data from one component to another component